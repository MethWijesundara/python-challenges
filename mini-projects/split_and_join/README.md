# A Simple String Splitter
A python exercise that demonstrates how to handle strings with multiple types of delimiters. 

## What it does
- Takes a string seperated by commas, colons and semicolons.
- Normalize the seperators using '.replace()'
- Converts the final string into a clean python list using '.split()'.
